"""Django configuration for the therapist dashboard domain."""

from django.apps import AppConfig


class TherapistDashboardConfig(AppConfig):
    """Configure the therapist dashboard boundary."""

    name = "therapist_dashboard"
